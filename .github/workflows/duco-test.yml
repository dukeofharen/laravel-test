name: Duco test

on:
  workflow_dispatch:

jobs:
  ReuseableMatrixJobForDeployment:
    strategy:
      matrix:
        environment: [test, staging]
    uses: dukeofharen/laravel-test/.github/workflows/reusable-workflow.yml@master
    with:
      target: ${{ matrix.target }}
    secrets:
      sshHost: ${{ secrets.SSH_HOST }}
