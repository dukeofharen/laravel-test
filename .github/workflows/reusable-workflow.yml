name: Reusable workflow example

on:
  workflow_call:
    secrets:
      token:
        required: true

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "Yo ${{ secrets.SSH_HOST }}"
